# v26.24.1

`pbir-cli` skill doc corrections (reports plugin), each reproduced on canvas
during a live Power BI Desktop verification session against pbir-cli:

- conditional-formatting.md: `value.color` corrected to `value.fontColor`
  for card visuals; icon names replaced with the verified PBIR vocabulary
  (`<Shape><Level>`, unknown names render as broken images); `--mid-value`
  documented for 3-color gradients; `--on values` added to the table icons
  example; "Changing CF Type" updated for builder upsert semantics
- thin-report-measures.md: extension measures must be homed on a table that
  exists in the model -- phantom tables break every referencing visual
- desktop-integration.md: screenshot scale caps at 3 and follows Desktop's
  current canvas zoom
- Versions: reports plugin and marketplace metadata bumped to 26.24.1
