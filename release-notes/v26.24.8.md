# v26.24.8

`pbir-cli` (reports plugin): drop the upgrade-nag guidance:

- Removed the "Version and availability" section that compared
  `pbir --version` against PyPI and pushed `pip install --upgrade`, plus the
  note that treated a missing `pbir desktop` command as the skill being ahead
  of the installed CLI. The skill no longer assumes the CLI lags behind it
- Versions: reports plugin, pbir-cli skill, and marketplace metadata bumped
  to 26.24.8
