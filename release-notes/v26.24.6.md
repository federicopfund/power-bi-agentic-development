# v26.24.6

`pbir-cli` (reports plugin): nudge users to keep the CLI up to date.

- The skill checked the installed `pbir` version against the latest on PyPI
  and suggested upgrading when behind, and treated a command the skill
  mentions but the CLI doesn't have as a sign the CLI is out of date rather
  than broken (this guidance was later removed in 26.24.8)
- Versions: reports plugin, pbir-cli skill, and marketplace metadata bumped
  to 26.24.6
