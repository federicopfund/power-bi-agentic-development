# v26.24.7

Keep the Fabric CLI up to date when publishing.

- The `pbir-cli` (reports) and `fabric-cli` skills now tell the agent to
  upgrade the Fabric CLI (`fab`) before publishing to Fabric, unless the user
  has pinned a specific version
- Versions: reports plugin, pbir-cli skill, and marketplace metadata bumped
  to 26.24.7; fabric-cli plugin and skill bumped to 26.24.1
